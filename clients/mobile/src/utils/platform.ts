/** Storage key for the user-configured Together server URL. */
export const SERVER_URL_KEY = "together_server_url";

/** Storage key for the JWT access token. */
export const TOKEN_KEY = "together_access_token";

/** Storage key for the JWT refresh token. */
export const REFRESH_KEY = "together_refresh_token";
