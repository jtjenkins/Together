{
  "name": "together-web",
  "projectType": "application",
  "sourceRoot": "clients/web/src",
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "options": { "script": "build" },
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
