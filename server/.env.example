# Database Configuration
DATABASE_URL=postgresql://together:together_dev_password@localhost:5432/together_dev

# Server Configuration
SERVER_HOST=127.0.0.1
SERVER_PORT=8080

# JWT Configuration - Required, minimum 32 characters. Fatal if missing.
JWT_SECRET=your-secret-key-change-this-in-production-min-32-chars

# App environment: set to "production" to enable restrictive CORS and other prod settings
# APP_ENV=production

# Production CORS: comma-separated list of allowed origins (only used when APP_ENV=production)
# ALLOWED_ORIGINS=https://your-domain.com,https://app.your-domain.com

# Logging
RUST_LOG=together_server=debug,sqlx=info,tower_http=debug
